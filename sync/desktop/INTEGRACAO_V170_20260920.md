# Desktop V170 — patch conservador preparado em 20/09/2026

Código original auditado: `src/services/MobileFullMirrorService.js` SHA256 `aa8a1ce09f0eb0f32d183af34ad45a4e4a910757c48741811eb504d824d959e6`; `src/ipc/sync.ipc.js` SHA256 `19a272f6e5e615d9595c48ec88ec91ed8071da88debd86be7ac46b1dcdff`. São códigos extraídos da auditoria de 15/09/2026, não prova de igualdade com a instalação Windows atual.

Patch local gerado e testado em arquivos originais completos, com SHA256 atualizado `711d01b5efe94a40dd01105028d5dae178996e35cff2544ead5d157f96486555` para o serviço e `484cd1fb74c458ecf6a76522853717e48d1c425394f8d3e07506773652b9c379` para o IPC. Não publicar a configuração `src/config/mobile-cloud.local.json`: contém credenciais privadas. Pacote separadamente entregue à administração, com instalador PowerShell que exige hashes EXATOS, Electron fechado, faz backup e rollback dos dois arquivos e não toca no SQLite.

Mudanças: (1) reconhecimento de origem `desktop-safe-v150` SOMENTE do dispositivo configurado; (2) confirmação sem reescrita da operação originada pelo próprio Desktop após SELECT por PK (inclusive composta), verificando dados, tombstone ou colisão; (3) colisão de identidade financeira vira CONFLITO, não UPSERT por ID; (4) mudança com base divergente não é sobrescrita; (5) resultados de importação/pull e status remoto entram no critério de sucesso, `lastHash` não avança quando há erro, pendência ou conflito; (6) IPC não considera `skipped` sucesso.

Testes isolados (sem rede e sem bancos reais): 10 testes com Node `node --test tests/desktop-v170.test.cjs`, 10 aprovados. Também foram aprovados `node --check` para ambos os arquivos. Não foi possível executar esse instalador no computador do usuário, validar o SQLite vivo, a UI nem o APK real neste ambiente.

**NÃO MESCLAR PR #29 como correção integral ainda:** o repositório GitHub contém só proposta de núcleo independente e este registro; o código Desktop completo testado está no pacote entregue, não integrado ao repositório Online. A Edge Function `gcmbs-sync-v131` em produção continua com defeito de baseline, diferença antiga entre snapshots idênticos e DELETE por ausência. Não atualizar nem instalar APK antes da correção da Edge e dos testes de paridade.

Progresso no banco de nuvem: 185 alterações V150 reconciliadas com ACK por correspondência exata/revisão; 40 chaves ausentes recuperadas após verificar que não havia tombstones/histórico e vínculo financeiro; 24 conflitos de timestamp sem diferença financeira resolvidos/arquivados; mais 51 ACKs de arredondamento semanticamente idênticos e duas mudanças bancárias confirmadas; 30 eventos financeiros com dados divergentes convertidos em CONFLITO em vez de ERRO, sem modificar saldo. Último contador observado: 13 alterações pendentes; 30 mudanças classificadas conflito correspondentes a 23 ocorrências de conflito abertas; seis ACKs de erro antigos (quatro comandos de checklist CANCELADOS, duas escalas de permutas). Banco de Horas 786 movimentos ativos; espelho `mobile_banco_horas` 786 linhas. Status final NÃO É sincronizado 100%.
