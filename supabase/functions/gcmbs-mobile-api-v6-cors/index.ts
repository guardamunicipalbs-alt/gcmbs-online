const TARGET='https://cxtayxzvilqrfczjlufk.supabase.co/functions/v1/gcmbs-communication-gateway-v73';
const cors={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'authorization,content-type,apikey,x-client-info','Access-Control-Allow-Methods':'POST,OPTIONS','Access-Control-Max-Age':'86400'};
Deno.serve(async(req:Request)=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});if(req.method!=='POST')return new Response(JSON.stringify({message:'Método não permitido.'}),{status:405,headers:{...cors,'Content-Type':'application/json; charset=utf-8'}});try{const body=await req.text();const headers:any={'Content-Type':'application/json'};const a=req.headers.get('authorization');if(a)headers.Authorization=a;const k=req.headers.get('apikey');if(k)headers.apikey=k;const r=await fetch(TARGET,{method:'POST',headers,body});const out=await r.arrayBuffer();const h=new Headers(cors);h.set('Content-Type',r.headers.get('content-type')||'application/json; charset=utf-8');h.set('X-GCMBS-Legacy-Proxy','10.0.73->canonical-gateway');h.set('Deprecation','true');return new Response(out,{status:r.status,statusText:r.statusText,headers:h});}catch(e){return new Response(JSON.stringify({message:e instanceof Error?e.message:'Falha no gateway canônico.'}),{status:502,headers:{...cors,'Content-Type':'application/json; charset=utf-8'}});}});
