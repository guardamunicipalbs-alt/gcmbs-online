import './v58-ui.js?v=100058';
import {MODULOS_GCMBS, normalizarPerfil, controleTotal} from './access-catalog.js';

const API='https://cxtayxzvilqrfczjlufk.supabase.co/functions/v1/gcmbs-mobile-api-v6';
const PUSH_API='https://cxtayxzvilqrfczjlufk.supabase.co/functions/v1/gcmbs-push-register';
const ACTIONS_V58='https://cxtayxzvilqrfczjlufk.supabase.co/functions/v1/gcmbs-actions-v58';
const QUADRO_V59='https://cxtayxzvilqrfczjlufk.supabase.co/functions/v1/gcmbs-quadro-v59';

export class AuthenticatedProvider {
  constructor(){ this.session=null; this.data=null; this.refs={viaturas:[],guardas:[],equipes:[],postos:[],tipos_escalas:[],eventos:[],oficios:[],grupos_ativacao:[],justificativas:[]}; }

  async call(action,payload={},authenticated=true){
    const headers={'Content-Type':'application/json'};
    const token=localStorage.getItem('gcmbs.mobile.token');
    if(authenticated && token) headers.Authorization=`Bearer ${token}`;
    const r=await fetch(API,{method:'POST',headers,body:JSON.stringify({action,...payload}),cache:'no-store'});
    let body={};try{body=await r.json()}catch{}
    if(!r.ok) throw new Error(body.message||`Erro ${r.status}`);
    return body;
  }
  async callV58(action,payload={}){const headers={'Content-Type':'application/json'};const token=localStorage.getItem('gcmbs.mobile.token');if(token)headers.Authorization=`Bearer ${token}`;const r=await fetch(ACTIONS_V58,{method:'POST',headers,body:JSON.stringify({action,...payload}),cache:'no-store'});let body={};try{body=await r.json()}catch{}if(!r.ok)throw new Error(body.message||`Erro ${r.status}`);return body;}
  async callQuadroV59(action,payload={}){const headers={'Content-Type':'application/json'};const token=localStorage.getItem('gcmbs.mobile.token');if(token)headers.Authorization=`Bearer ${token}`;const r=await fetch(QUADRO_V59,{method:'POST',headers,body:JSON.stringify({action,...payload}),cache:'no-store'});let body={};try{body=await r.json()}catch{}if(!r.ok)throw new Error(body.message||`Erro ${r.status}`);return body;}
  async login(identificador,senha){const body=await this.call('login',{identificador,senha},false);localStorage.setItem('gcmbs.mobile.token',body.token);this.session=body.session;await this.load();return body.session;}
  async restore(){const token=localStorage.getItem('gcmbs.mobile.token');if(!token)return null;try{const body=await this.call('session');this.session=body.session;await this.load();return this.session;}catch{localStorage.removeItem('gcmbs.mobile.token');this.session=null;this.data=null;return null;}}
  async logout(){try{await this.call('logout')}catch{}localStorage.removeItem('gcmbs.mobile.token');this.session=null;this.data=null;}
  async load(){const body=await this.call('data');this.data=body;try{this.refs=await this.call('references')}catch{this.refs={viaturas:[],guardas:[],equipes:[],postos:[],tipos_escalas:[],eventos:[],oficios:[],grupos_ativacao:[],justificativas:[]};}if(this.pode('escalas')||this.pode('relatorios')){try{const mirror=await this.entityList('escalas',5000,0);const rows=(mirror.records||[]).map(r=>r.data||{});if(rows.length)this.data.escalas=rows;}catch(e){console.warn('[GCMBS] relatório usando réplica móvel resumida:',e?.message||e);}}return this;}
  guardas(){return this.data?.guardas||[]} escalas(){return this.data?.escalas||[]} extras(){return this.data?.extras||[]} permutas(){return this.data?.permutas||[]} bancoHoras(){return this.data?.banco_horas||[]} notifications(){return this.data?.notifications||[]} institutionalNotices(){return this.data?.institutional_notices||[]} actionRequests(){return this.data?.action_requests||[]} permutationCandidates(){return this.data?.permutation_candidates||[]}
  async branding(){return (await this.call('branding')).branding||[]} async entityCatalog(){return (await this.call('entity_catalog')).entities||[]} async entityList(entity,limit=500,offset=0){return this.call('entity_list',{entity,limit,offset})} async entityMutate(entity,record_key,operation,data,client_change_id=''){return this.call('entity_mutate',{entity,record_key,operation,data,client_change_id})}
  async quadro(data){const base=await this.call('quadro_operacional',{data});try{const c=await this.callQuadroV59('quadro_counters',{data});if(base?.efetivo){base.efetivo.servicoA=Number(c.servicoA||0);base.efetivo.servicoB=Number(c.servicoB||0);if(base.efetivo.detalhes){base.efetivo.detalhes.servicoA=c.detalhesA||[];base.efetivo.detalhes.servicoB=c.detalhesB||[];}}}catch(e){console.warn('[GCMBS] contadores ordinários + extras:',e?.message||e);}return base}
  async relatorioEscalas(){return (await this.call('relatorio_escalas')).escalas||[]} async ajustarEscalaComando(adjustment){const r=await this.callV58('admin_scale_adjust_v58',{adjustment});await this.load();return r} async permutaCandidatesFor(data,turno){return this.call('permuta_candidates',{data,turno})} async extraSwapCandidates(){return this.callV58('extra_swap_candidates',{})} async frequencyServices(guarda_id,data){return this.call('frequency_services',{guarda_id,data})} async checklistContext(viatura_id){return this.call('checklist_context',{viatura_id})} async occurrenceContext(data,hora){return this.call('occurrence_context',{data,hora})}
  references(){return this.refs||{viaturas:[],guardas:[],equipes:[],postos:[],tipos_escalas:[],eventos:[],oficios:[],grupos_ativacao:[],justificativas:[]}}
  async requestBankCorrection(request){const r=await this.call('request_bank_correction',{request});await this.load();return r} async requestPermuta(request){const modalidade=String(request?.modalidade||'ASSUNCAO').toUpperCase();const r=modalidade==='TROCA_EXTRA'?await this.callV58('request_extra_swap',{request}):await this.call('request_permuta',{request});await this.load();return r} async acceptExtraSwap(id,aceitou){const r=await this.callV58('accept_extra_swap',{id,aceitou});await this.load();return r} async updatePermutaRequest(id,request){const r=await this.call('update_permuta_request',{id,request});await this.load();return r} async cancelPermutaRequest(id){const r=await this.call('cancel_permuta_request',{id});await this.load();return r} async decidePermutaRequest(id,decisao,motivo=''){const r=await this.call('decide_permuta_request',{id,decisao,motivo});await this.load();return r} async adminDeletePermutaRequest(id,motivo=''){const r=await this.call('admin_delete_permuta_request',{id,motivo});await this.load();return r} async decideBankRequest(id,decisao,ajustes={}){const r=await this.call('decide_bank_request',{id,decisao,ajustes});await this.load();return r} async sendInstitutionalMessage(message){const r=await this.callV58('send_message_v58',{message});await this.load();return r}
  async markNotificationRead(id){const r=await this.call('mark_notification_read',{id});if(r.success&&this.data){const n=this.data.notifications?.find(x=>Number(x.id)===Number(id));if(n&&!n.lida_em)n.lida_em=new Date().toISOString();}return r}
  async pushCall(payload={}){const token=localStorage.getItem('gcmbs.mobile.token');if(!token)throw new Error('Sessão móvel não autenticada.');const r=await fetch(PUSH_API,{method:'POST',headers:{'Content-Type':'application/json','Authorization':`Bearer ${token}`},body:JSON.stringify(payload),cache:'no-store'});let b={};try{b=await r.json()}catch{}if(!r.ok)throw new Error(b.message||`Erro ${r.status}`);return b;}
  async registerPushToken(token,meta={}){return this.pushCall({action:'register',token,...meta})} async unregisterPushToken(token=''){return this.pushCall({action:'unregister',token})}
  meta(){return this.data?.meta||{}} permissoes(){return this.session?.permissoes||[]} perfil(){return normalizarPerfil(this.session||{})} controleTotal(){return controleTotal(this.session||{})} gestor(){const p=this.perfil();return p==='comandante'||p==='subcomandante'}
  permissoesEfetivas(){if(this.controleTotal())return MODULOS_GCMBS.map(x=>({modulo:x.id,nivel:'EDICAO'}));return this.permissoes().filter(p=>p&&p.modulo&&p.modulo!=='*'&&p.ativo!==false);}
  modulosAutorizados(){const mapa=new Map(this.permissoesEfetivas().map(p=>[p.modulo,String(p.nivel||'CONSULTA').toUpperCase()]));return MODULOS_GCMBS.filter(x=>mapa.has(x.id)).map(x=>({...x,nivel:mapa.get(x.id)}));}
  pode(modulo,nivel='CONSULTA'){if(modulo==='escalas'&&!this.gestor())return false;if(modulo==='relatorios_frota'&&!this.gestor())return false;if(this.controleTotal())return true;const rank={CONSULTA:1,EDICAO:2};const alvo=rank[String(nivel).toUpperCase()]||1;return this.permissoes().some(p=>p?.ativo!==false&&(p.modulo===modulo||p.modulo==='*')&&(rank[String(p.nivel).toUpperCase()]||0)>=alvo);}
}
