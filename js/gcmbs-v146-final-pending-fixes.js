/* GCMBS V146 — reconciliação defensiva das pendências do Comando. */
(()=>{'use strict';if(window.__GCMBS_V146__)return;window.__GCMBS_V146__=true;
const API='https://cxtayxzvilqrfczjlufk.supabase.co/functions/v1/gcmbs-communication-gateway-v131',norm=v=>String(v??'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').trim().toUpperCase(),token=()=>localStorage.getItem('gcmbs.mobile.token')||'';
const final=s=>['PROCESSADO','APROVADA','APROVADO','NEGADA','NEGADO','RECUSADA','RECUSADO','CANCELADA','CANCELADO','SUPERADO','ERRO'].includes(norm(s));
async function getData(){const t=token();if(!t)return null;const r=await fetch(API,{method:'POST',headers:{'Content-Type':'application/json','Authorization':`Bearer ${t}`},body:JSON.stringify({action:'data'}),cache:'no-store'});return r.ok?r.json():null}
function identity(r){const p=r?.payload||{};return [Number(r?.guarda_id||p.guarda_id||0),norm(p.competencia),String(p.data_servico||p.data||'').slice(0,10)].join('|')}
function reconcileBank(d){const req=(d?.action_requests||[]).filter(x=>norm(x.tipo)==='BANCO_HORAS_CORRECAO'),dec=(d?.action_requests||[]).filter(x=>norm(x.tipo)==='BANCO_HORAS_DECISAO_COMANDO');const decided=new Set(dec.filter(x=>final(x.status)&&['APROVADA','NEGADA'].includes(norm(x.payload?.decisao))).map(x=>String(x.payload?.request_id||'')));const byIdent=new Set(dec.filter(x=>final(x.status)&&norm(x.payload?.decisao)==='APROVADA').map(identity));document.querySelectorAll('[data-cmd-bh-ok],[data-cmd-bh-no]').forEach(b=>{const card=b.closest('.request-card,.card,article,section,div');if(!card)return;const id=String(b.dataset?.cmdBhOk||b.dataset?.cmdBhNo||card.dataset?.requestId||'');const text=norm(card.textContent);const r=req.find(x=>String(x.id)===id);const done=(id&&decided.has(id))||(r&&byIdent.has(identity(r)));if(done){card.querySelectorAll('[data-cmd-bh-ok],[data-cmd-bh-no]').forEach(x=>x.remove());const badge=card.querySelector('.badge,.status-badge');if(badge){badge.textContent='APROVADA';badge.classList.remove('pending','warning');badge.classList.add('success')}card.dataset.v146Reconciled='1'}else if(text.includes('APROVADA')||text.includes('PROCESSADO'))card.querySelectorAll('[data-cmd-bh-ok],[data-cmd-bh-no]').forEach(x=>x.remove())})}
function reconcilePermuta(d){const req=(d?.action_requests||[]).filter(x=>norm(x.tipo)==='PERMUTA');document.querySelectorAll('[data-cmd-pm-ok]').forEach(b=>{const card=b.closest('.request-card,.card,article,section,div');if(!card)return;const id=String(b.dataset.cmdPmOk||card.dataset.requestId||'');const r=req.find(x=>String(x.id)===id);if(r&&!r.desktop_referencia_id){b.disabled=true;b.title='Aguardando vínculo operacional confirmado no Desktop';b.textContent='Aguardando sincronização';card.dataset.v146Unlinked='1'}})}
function version(){document.querySelectorAll('body *').forEach(el=>{if(el.children.length)return;const t=(el.textContent||'').trim();if(/^Online\s*·?\s*(Online\s*)?10\.0\.85$/i.test(t))el.textContent='Online · 10.0.146'});document.documentElement.dataset.gcmbsVersion='10.0.146'}
async function refresh(){version();const d=await getData();if(!d)return;reconcileBank(d);reconcilePermuta(d)}
function boot(){refresh();let q=false;new MutationObserver(()=>{if(q)return;q=true;requestAnimationFrame(()=>{q=false;refresh()})}).observe(document.documentElement,{childList:true,subtree:true});setInterval(refresh,12000)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();})();
