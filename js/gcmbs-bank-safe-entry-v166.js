/* V166 — substitui ações antigas conflitantes por acesso ao painel isolado. */
(()=>{'use strict';if(window.Capacitor?.isNativePlatform?.())return;
function mount(){const card=document.getElementById('bancoGestaoCard');if(!card)return;
 let box=document.getElementById('gc166-safe-entry');if(!box){box=document.createElement('div');box.id='gc166-safe-entry';box.className='notice';box.style.cssText='margin:12px 0 16px;padding:14px;border:1px solid #9fc4e7;border-radius:12px;background:#edf6ff;display:grid;gap:9px';const label=document.createElement('strong');label.textContent='Aprovações e correções — painel independente';const desc=document.createElement('span');desc.textContent='Para evitar interferência dos controles antigos, as decisões agora são feitas na tela isolada, usando a mesma sessão do Comando.';const link=document.createElement('a');link.href='banco-aprovacoes-v166.html';link.textContent='Abrir aprovação, correção e recusa';link.style.cssText='display:inline-flex;justify-content:center;align-items:center;width:fit-content;min-height:44px;padding:11px 17px;border-radius:10px;background:#135eaa;color:#fff;text-decoration:none;font-weight:750';box.append(label,desc,link);const host=document.getElementById('listaBancoGestao');(host||card).before(box)}
 const old=card.querySelectorAll('#listaBancoGestao .request-actions');old.forEach(x=>{x.hidden=true;x.style.setProperty('display','none','important')});}
function boot(){mount();let scheduled=false;new MutationObserver(()=>{if(scheduled)return;scheduled=true;requestAnimationFrame(()=>{scheduled=false;mount()})}).observe(document.documentElement,{childList:true,subtree:true});}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();